import { css } from "@emotion/react";

const reset = css`
  body {
    width: 100vw;
    height: calc(100vh - 8vh);
    margin: 0px;
    background: white;
    font-size: 16px;
  }
`;

export default reset;